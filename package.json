{
  "name": "kgp-lost-found",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "vercel-build": "cd server && npm install && npx prisma generate && npx prisma db push --accept-data-loss && cd ../client && npm install && npm run build"
  }
}
